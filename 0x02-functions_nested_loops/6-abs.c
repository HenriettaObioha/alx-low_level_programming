#include "main.h"

/**
 * _abs - This program computes the absolute value of an integer
 * @i: int to check on code
 * Return: the absolute value of the int
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);
}
